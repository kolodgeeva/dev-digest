# DevDigest API base URL the MCP server talks to (must be running).
DEVDIGEST_API_URL=http://localhost:3001

# Optional: how long run_agent_on_pr polls the run outcome before returning
# { status: "running", run_id } for the caller to fetch later (ms).
# MCP_RUN_TIMEOUT_MS=120000
