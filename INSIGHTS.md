# INSIGHTS — root (cross-cutting)

Append-only notes the previous session leaves for the next — engineering insights
that don't belong to a single package (project-wide conventions, cross-package
decisions, tooling that spans server+client). Per-package insights go in that
package's own `INSIGHTS.md`. Maintained by the `engineering-insights` skill — see
`.claude/skills/engineering-insights/SKILL.md`.

**How to use:** don't overwrite; correct a stale entry with a dated note referencing
the old one. Each entry: `YYYY-MM-DD · category · insight · evidence (file:line)`.
Write it actionable and "cold" — readable with zero session context.

**Maintenance:** prune fixed/obsolete entries monthly; resolve contradictions
explicitly; split into domain files (e.g. `INSIGHTS-ci.md`) past ~200 entries. This
is a draft under human spot-check — committed to git.

## What Works
<!-- approaches & solutions that worked -->

## What Doesn't Work
<!-- dead ends & antipatterns — the most-skipped, most-valuable section; fill it -->
- 2026-06-21 · antipattern · Can't smoke-test a `PreToolUse` Bash hook whose matcher keys on a substring (e.g. `git commit`) by piping a fake event through the Bash tool — your *own* test invocation's command string contains that substring, so the live hook denies your Bash call before it runs. Put the test logic + the trigger string in a script file and run `bash /tmp/x.sh` (the outer command the hook sees is just `bash /tmp/x.sh`, which doesn't match). Evidence: `.claude/settings.json` PreToolUse hook added this session.

## Codebase Patterns
<!-- conventions, architectural decisions -->
- 2026-06-27 · codebase-pattern · Two new subagents added: `.claude/agents/planner.md` (model opus, read-only + writes only `.claude/plans/<slug>.md`) and `.claude/agents/implementer.md` (model sonnet, edits code). Intended flow: planner emits a Development Plan with a **File ownership** section that partitions work into NON-OVERLAPPING file sets (typically `server/**` vs `client/**`) → multiple `implementer`s run in parallel **on the same branch** (no worktree isolation, by request) and each stays inside its slice's files. Implementer self-reviews only its own diff + runs `pnpm typecheck` and unit tests (`--exclude '**/*.it.test.ts'`); it is NOT the quality gate — `pr-self-review` still runs at commit. If you change the parallelism model, the non-overlap invariant is what prevents same-branch write collisions.
- 2026-06-27 · codebase-pattern · Planning a **meta/tooling** task (authoring `.claude/agents/*.md`, not feature code) breaks two planner defaults: (1) the `[SERVER]/[CLIENT]/[SHARED]/[TEST-*]` task labels don't fit — use `[AGENT]`/`[DOCS]` instead; (2) the file→skill routing table keys off `*.ts`/`*.tsx`, so it does NOT trigger for markdown deliverables — only `mermaid-diagram` is relevant, and only if a diagram is added. Acceptance criteria must be **file-shape checks only** (`ls`/`grep`/YAML-parse), never `pnpm build/test`, because nothing compiles. Precedent: `.claude/plans/new-agents.md` (the plan for the four new agents). Reuse this shape for any future "author agents/skills/docs" plan.
- 2026-06-27 · codebase-pattern · Two distinct review-finding vocabularies now coexist by design — keep them separate when authoring reviewer agents. `architecture-reviewer` (and `pr-self-review`) emit `CRITICAL | WARNING | SUGGESTION` (mirrors `server/src/vendor/shared/contracts/findings.ts`). `plan-verifier` is requirements-traceability, NOT quality, so it emits `COVERED | PARTIAL | MISSING` via three-level verification (L1 file exists → L2 substantive impl not a stub → L3 wired/imported/registered), parsing the planner's plan sections **6** (task checklist), **8** (file ownership), **9** (acceptance criteria). Don't make plan-verifier flag style/architecture — that's architecture-reviewer's job. Spec: `.claude/plans/new-agents.md` §6.2–6.3.
- 2026-06-27 · codebase-pattern · The canonical "changed file path → which skills apply" map lives ONCE in `.claude/skills/pr-self-review/SKILL.md` (Step 2 table, ~line 51). Both new agents (planner, implementer) embed a COPY of that same table in their prompt bodies. If you add/rename a skill or change a path bucket, update all three in lockstep (pr-self-review + planner.md + implementer.md), or routing silently diverges.
- 2026-06-17 · codebase-pattern · Zod contracts in `*/vendor/shared/` have NO sync script — `server/src/vendor/shared/` and `client/src/vendor/shared/` are independent copies you must edit identically. Adding/removing one field = two edits (both `trace.ts`, both `platform.ts`, …). Precedent: commit d45ab0d edited both `trace.ts` copies in one go. Forgetting one side compiles in that package but fails the other's typecheck/`.parse`.
- 2026-06-21 · codebase-pattern · `.claude/skills/README.md` preamble claims a `.cursor/skills/ → ../.claude/skills` symlink exists for Cursor — it does NOT (no `.cursor/` dir in the repo). Skills are picked up from `.claude/skills/` directly. If Cursor compatibility is wanted, create the symlink (`ln -s ../.claude/skills .cursor/skills`); don't assume it's there. New skill added this session: `.claude/skills/frontend-architecture/` (code-placement/folder-structure; SKILL.md + structure.md + README.md), registered in the catalog table. Its scope is deliberately disjoint from `react-best-practices` (hooks/perf) and `next-best-practices` (RSC/data) — keep those boundaries when editing any of the three.

## Tool & Library Notes
<!-- dependency quirks/gotchas -->
- 2026-06-21 · tooling · Root `CLAUDE.md` is a symlink to `AGENTS.md` (`readlink -f CLAUDE.md` → `AGENTS.md`). The Edit/Write tools refuse to write through a symlink — edit `AGENTS.md` directly. (Per-package `server/CLAUDE.md` etc. are real files.)
- 2026-06-21 · tooling · New `pr-self-review` skill + commit gate added this session. `git commit` is blocked by a `PreToolUse` hook in `.claude/settings.json` until a pass flag `/tmp/claude-pr-selfreview-<diffhash>` exists; the skill (`/pr-self-review`) writes that flag only when 0 CRITICAL findings. Flag is keyed to a hash of `git diff $(git merge-base main HEAD)` — NOT session id — because the hook reads `.session_id` from stdin JSON while the skill's Bash env has no reliable matching session var; diff-hash keying also forces re-review whenever code changes. The skill routes changed files to existing skills by path (UI→frontend-architecture/next/react, server→onion/fastify/drizzle/pg, all→typescript-expert/zod/security). To bypass during an authorized commit, create the flag for the current diff hash.
- 2026-06-21 · tooling · CORRECTION to the `pr-self-review` note above. Two changes after first use: (1) **Scope is now local changes vs the current branch tip `HEAD`** (staged + unstaged + untracked), NOT vs `main` — committed branch work isn't re-reviewed. (2) **The fingerprint is computed through a throwaway index**, not `git diff <merge-base>`. The old hash excluded untracked files, so a new file slipped past review AND `git add`-ing it shifted the hash → guaranteed one "blocked" cycle on every new file. Fix (same in both skill Step 1/5 and the hook): `tmp=$(mktemp); GIT_INDEX_FILE="$tmp" git read-tree HEAD; GIT_INDEX_FILE="$tmp" git add -A; h=$(GIT_INDEX_FILE="$tmp" git diff --cached HEAD | shasum | cut -d' ' -f1); rm -f "$tmp"`. This is staging-invariant (verified) and includes untracked files. The real index is never touched (temp `GIT_INDEX_FILE`).

## Tool & Library Notes (cont.)
- 2026-06-28 · tooling · The system `python3` has **no PyYAML** (`ModuleNotFoundError: No module named 'yaml'`). The `new-agents.md` plan's §10 acceptance check #2 verifies agent frontmatter via `python3 -c 'import yaml; ...'` — it fails on this machine. When validating `.claude/agents/*.md` frontmatter, use grep/awk instead (e.g. `grep '^tools:\|^model:\|^name:'`), or `pip install pyyaml` first. Don't trust a green frontmatter check that silently swallowed the import error. Hit while running acceptance checks for the four new agents.
- 2026-06-27 · tooling · Do NOT add a `paths:` frontmatter field to a `SKILL.md` to auto-scope it to matching files — it's bugged (claude-code Issue #49835): the skill becomes undiscoverable ("Unknown skill: <name>", absent from `/` autocomplete). Route file→skill via a table in the *agent's* prompt body instead (as `pr-self-review`, `planner`, `implementer` do). Verified against docs/issue this session; re-test if a much newer Claude Code claims a fix.
- 2026-06-27 · tooling · 2026 research to bake into any test-generating agent's prompt as an explicit forbid-list: AI agents systematically **over-mock** (arxiv 2602.00409 — 36% of agent test commits add mocks vs 26% human; agents use mocks for ~95% of doubles vs humans' mix of fakes/spies) and write **tautological assertions** (assert a mock returns what you told it to). The `test-writer` agent body therefore hard-FORBIDS: mocking the subject under test / your own modules/hooks/context, snapshot tests, `fireEvent` (use `userEvent.setup()`), `container.querySelector` (use `screen` role/label queries), importing from `jest`, asserting `useState`/internal hook state, and tautological assertions. Mock only at boundaries (network/adapters/external SDKs). Spec: `.claude/plans/new-agents.md` §6.1.
- 2026-06-27 · tooling · A subagent's `skills:` frontmatter list (used in `.claude/agents/{planner,implementer}.md`) **preloads the full content of each listed skill into the agent's context at startup** — it is NOT lazy. Both agents list the full backend+UI+cross-cutting skill set; planner additionally lists `mermaid-diagram`. Rationale: planner plans the implementation and assigns skills per task, so it needs the full content of every skill the implementer will use, not just the names — by design the planner's skill set is a superset of the implementer's. This is separate from on-demand `Skill` tool invocation, which both agents also use for the routing-table lookups.

## Recurring Errors & Fixes
<!-- repeated error + its fix -->

## Session Notes
<!-- datestamped session summaries -->
- 2026-06-27 · Designed + built `planner` and `implementer` subagents (researcher + Explore recon, web best-practices from Anthropic agent guides + Claude Code docs). Mirrored `researcher.md` shape (English prompt body, reply in request's language). Key user constraints baked in: implementer self-reviews only its own code + makes tests pass (not the full gate), reads module INSIGHTS.md just-in-time while planner folds cross-cutting insights at plan time (hybrid), no worktree isolation (same-branch + file-ownership), skills declared in `skills:` frontmatter AND linked as `[name](.claude/skills/<name>/SKILL.md)` in routing tables.

- 2026-06-27 · Planned (not yet built) four more subagents to extend the `researcher→planner→implementer` pipeline: `test-writer` (write, both backend+frontend tests), `architecture-reviewer` (read-only, onion+frontend layering, CRITICAL/WARNING/SUGGESTION, diff-scoped), `plan-verifier` (read-only, requirements-traceability COVERED/PARTIAL/MISSING), `doc-writer` (write, Diátaxis + Mermaid, with a doc-placement table and do-not-clobber list). Grounded by 3 parallel `researcher` agents (2026 sources: arxiv 2602.00409 over-mocking, Feb-2026 agentic-architecture playbook's L1/L2/L3 verification, Diátaxis). Plan lives at `.claude/plans/new-agents.md` with parallel file-ownership slices A–D (one agent file each) + serial slice E (README). Next step if resumed: launch 4 implementers on slices A–D, then E.

- 2026-06-28 · Executed `.claude/plans/new-agents.md`: launched 4 `implementer` subagents in parallel on slices A–D (each authoring one `.claude/agents/*.md` — test-writer, architecture-reviewer, plan-verifier, doc-writer), then ran slice E (README catalog + prose subsections). The parallel-slices model worked cleanly — non-overlapping single-file ownership meant zero collisions, and each implementer self-ran its slice's §10 checks. Slice E (README, the shared/dependent file) correctly serialized after A–D. All four agents are now registered + routable. Gotcha encountered: §10 check #2 (PyYAML) fails on this box — see Tool note above. Nothing committed (awaiting user approval per project rule).

## Open Questions
<!-- what's still unresolved -->
